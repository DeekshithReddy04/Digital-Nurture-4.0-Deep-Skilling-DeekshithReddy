Java Persistence API (JPA)

- JPA is a **Java specification** (JSR 338) for object-relational mapping (ORM).
- It provides standard annotations and interfaces to interact with databases.
- **Does not provide any implementation** on its own.
- Needs a provider like Hibernate to work.